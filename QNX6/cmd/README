This directory was used to prototype the new command server architecture.
Each experiment's command server is actually a custom compilation, so
much of this functionality has been moved into libraries.
