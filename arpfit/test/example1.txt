PARAM k[2];
INPUT x, y;

x = [ 1:100 ];
y = 3*x + 5 + .7*randn(100);

Function linfit( PARAM k, INDEPENDENT x ) {
  Fix k[0] if k[1] > 7;
  Constrain k[0] >= 0;
  File "Linfit.log";
  Return k[0]+k[1]*x;
}
Fit y To linfit(k,x);
